## 1. 配置与启动保护

- [ ] 1.1 在 `agent/internal/config` 和部署配置模板中新增 `admin_auth` 配置结构，支持用户名、密码哈希、会话密钥和会话 TTL。
- [ ] 1.2 在 agent 启动阶段校验 admin 鉴权配置完整性，避免在启用后台时以不安全默认配置启动。
- [ ] 1.3 提供管理员密码哈希生成说明或辅助脚本，并更新部署文档。

## 2. 后端鉴权与路由保护

- [ ] 2.1 实现管理员登录、会话查询、登出接口，以及基于签名 Cookie 的会话校验逻辑。
- [ ] 2.2 为 admin 管理 API 增加统一保护层，把受保护接口与公开接口明确分组挂载。
- [ ] 2.3 为 admin SPA 静态资源与前端路由回退增加同一套鉴权保护，确保未登录无法直接打开后台页面。
- [ ] 2.4 为登录成功、登录失败、未认证访问、配置缺失等关键路径补充后端测试。

## 3. 前端登录流与会话感知

- [ ] 3.1 在 admin 前端新增登录页、认证状态管理和受保护路由守卫。
- [ ] 3.2 更新 API 客户端以携带 Cookie、处理 401，并接入会话查询、登录、登出接口。
- [ ] 3.3 在现有页面加载流程中接入会话探测、登录成功跳回原页面、登出后返回登录页的行为。
- [ ] 3.4 为关键认证交互补充前端测试或最小可回归验证用例。

## 4. 联调与发布验证

- [ ] 4.1 验证未登录状态下 admin 页面与受保护管理 API 均返回未认证结果。
- [ ] 4.2 验证登录后原有后台能力可正常使用，包括会话查看、配置编辑和技能管理等核心路径。
- [ ] 4.3 验证会话过期或登出后，前端会清理登录态并重新要求认证。
