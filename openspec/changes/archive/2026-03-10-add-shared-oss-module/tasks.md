## 1. 模块骨架与配置

- [x] 1.1 选定共享 OSS 模块落点并创建公共接口、配置结构和基础目录
- [x] 1.2 接入 MinIO/S3 兼容客户端依赖，并封装客户端初始化逻辑
- [x] 1.3 为 endpoint、bucket、access key、secret key、TLS、超时和对象前缀增加统一配置读取与校验

## 2. 上传与下载能力

- [x] 2.1 实现对象上传接口，支持显式 object key、可选 Content-Type 和流式内容输入
- [x] 2.2 实现对象 key 生成策略，支持未显式传 key 时按配置前缀生成稳定对象路径
- [x] 2.3 实现对象下载接口，返回可读取内容和必要元数据
- [x] 2.4 为对象不存在、鉴权失败、网络超时和内部 I/O 异常建立统一错误分类

## 3. 可测试性与接入准备

- [x] 3.1 将共享 OSS 能力定义在可注入接口后，提供 mock/fake 替身以支持单元测试
- [x] 3.2 为客户端初始化、上传、下载和错误分类补充单元测试
- [x] 3.3 增加一份面向调用方的接入示例或说明，明确如何通过公共模块完成上传和下载
